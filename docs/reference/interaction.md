# Interaction settings

These options control how users interact with the 3D structure. The raycast thresholds
determine how forgiving clicking/tapping on thin geometry is; touch thresholds are
larger than mouse thresholds by default.

<OptionsTable group="interaction" />
