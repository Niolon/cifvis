# Unit-cell visualization settings

These options style the unit-cell box and crystallographic axis arrows used by the cell
growth modes (`symmetryMode: 'cell'` / `'fragment-cell'`).

<OptionsTable group="cell" />
