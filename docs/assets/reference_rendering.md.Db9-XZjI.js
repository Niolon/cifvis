import{_ as o,C as n,o as s,c as i,a4 as r,E as d}from"./chunks/framework.DGN0vxDu.js";const f=JSON.parse('{"title":"Rendering settings","description":"","frontmatter":{},"headers":[],"relativePath":"reference/rendering.md","filePath":"reference/rendering.md"}'),a={name:"reference/rendering.md"};function l(c,e,p,h,u,g){const t=n("OptionsTable");return s(),i("div",null,[e[0]||(e[0]=r('<h1 id="rendering-settings" tabindex="-1">Rendering settings <a class="header-anchor" href="#rendering-settings" aria-label="Permalink to &quot;Rendering settings&quot;">​</a></h1><p>These options control how the 3D structure is rendered.</p><h2 id="render-styles" tabindex="-1">Render styles <a class="header-anchor" href="#render-styles" aria-label="Permalink to &quot;Render styles {#render-styles}&quot;">​</a></h2><p><code>adpRepresentation</code> selects the physical anisotropic-displacement surface:</p><ul><li><strong><code>ellipsoid</code></strong> (default) — the conventional equal-probability ellipsoid;</li><li><strong><code>rmsd-peanut</code></strong> — a radial RMS displacement surface with <code>r(n) = peanutScale × sqrt(|nᵀUn|)</code>. Positive directions use the element&#39;s <code>atomColor</code> for the body and <code>ringColor</code> for the grid. For difference tensors with negative directions, a complementary surface is drawn with those colours switched. Its radius has units of length after scaling and must not be interpreted as an enclosed probability.</li></ul><p>PEANUT grid density and physical stroke width can be adjusted independently with <code>peanutMeridianCount</code>, <code>peanutLatitudeIntervals</code>, and <code>peanutGridLineWidth</code>. The line width is measured in Å, so its apparent screen width changes naturally with zoom. <code>peanutGridPoleAxis</code> selects either a common structure Y pole or an atom-local maximum, intermediate, or minimum principal-displacement axis. <code>peanutDetail</code> controls the shared PEANUT sphere tessellation independently of <code>atomDetail</code>; its higher default keeps signed zero-crossing lobes smooth.</p><p><code>renderStyle</code> independently selects how that surface is presented (live comparison in <a href="./../widget/display-options.html#ortep-render-modes">Widget → Display options</a>):</p><ul><li><strong><code>solid-3d</code></strong> — a smooth, lit solid surface.</li><li><strong><code>cutout-3d</code></strong> — a missing-octant ellipsoid, or a solid PEANUT with an orderly surface grid in the element&#39;s <code>ringColor</code>.</li><li><strong><code>cutout-2d</code></strong> — a hatched ellipsoid cutout, or a PEANUT grid with hidden rear lines removed and a separate expanded silhouette outline. PEANUT grid strokes have a physical structure-space width, while the silhouette keeps a constant screen-space width. Existing publication colour and bond rules apply.</li></ul><p>All PEANUT modes use shared sphere topology and instancing. There are no PEANUT cutouts.</p><p><code>renderMode: &#39;onDemand&#39;</code> (the default) renders only when needed — call <code>viewer.requestRender()</code> after changing external state from your own code; <code>&#39;constant&#39;</code> renders continuously.</p><p>In the cutout styles, <code>sealCutoutCavity</code> (default on) fills the removed octant in the depth buffer so a neighbouring atom or bond that lies inside the carved-open cavity is occluded instead of showing through, while the exposed cross-section stays visible. In both cutout styles, complete ellipsoids are additionally ordered by their nearest camera-facing extent and committed to an ellipsoid-only stencil. The 2D mask includes the expanded outline; the 3D mask follows the exact surface silhouette. Overlapping ellipsoids therefore do not intersect one another, while bonds and isosurfaces continue to use normal physical depth testing.</p>',11)),d(t,{group:"rendering"})])}const y=o(a,[["render",l]]);export{f as __pageData,y as default};
