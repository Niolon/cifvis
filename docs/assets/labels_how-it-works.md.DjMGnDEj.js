import{_ as a,o,c as t,a4 as r}from"./chunks/framework.CyI4nVbD.js";const m=JSON.parse('{"title":"How label placement works","description":"","frontmatter":{},"headers":[],"relativePath":"labels/how-it-works.md","filePath":"labels/how-it-works.md"}'),n={name:"labels/how-it-works.md"};function i(s,e,l,c,d,h){return o(),t("div",null,[...e[0]||(e[0]=[r('<h1 id="how-label-placement-works" tabindex="-1">How label placement works <a class="header-anchor" href="#how-label-placement-works" aria-label="Permalink to &quot;How label placement works&quot;">​</a></h1><p>This page explains the algorithms behind the <code>atomLabels</code> options in principle, so the ~40 tuning knobs in the <a href="./../reference/atom-labels.html">reference table</a> have context. You rarely need to change any of them — but when a dense structure omits labels you wanted, or a layout takes too long, this is the mental model to debug with.</p><h2 id="the-problem" tabindex="-1">The problem <a class="header-anchor" href="#the-problem" aria-label="Permalink to &quot;The problem&quot;">​</a></h2><p>Labels live in <strong>screen space</strong>: 2D text boxes drawn over a 3D projection that changes with every rotation and zoom. A good layout must place each label close to its atom, avoid covering atoms, bonds, ADP rings, and other labels, keep the viewport edges clear, and stay <em>stable</em> — labels should not jump around between similar poses. Since a dense projection may simply have no room, every mode is an <em>omission</em> mode at heart: a label with no acceptable position is dropped (and reported via <code>getAtomLabelLayout()</code>) rather than drawn misleadingly.</p><h2 id="candidate-scoring" tabindex="-1">Candidate scoring <a class="header-anchor" href="#candidate-scoring" aria-label="Permalink to &quot;Candidate scoring&quot;">​</a></h2><p>For each requested atom, the placer enumerates candidate positions around the projected atom (near positions first, then farther fallback rings at <code>fallbackDistance</code> spacing). Every candidate is scored: collisions with atoms, bonds, and existing labels are rejected outright using the configured clearances (<code>atomPadding</code>, <code>bondPadding</code>, <code>labelPadding</code>, <code>viewportPadding</code>), and soft penalties rank the survivors:</p><ul><li><code>ringPenalty</code> — interference with ADP rings is strongly discouraged.</li><li><code>movementPenalty</code> — a candidate far from the label&#39;s previous position costs extra, which is what keeps layouts stable across interaction.</li><li><code>leaderBondCrossingPenalty</code> — a leader line that would cross a displayed bond costs extra.</li></ul><p>If the winning candidate is farther than a comfortable distance, a <strong>leader line</strong> is drawn from the text to the atom (<code>leaderLines: &#39;auto&#39;</code>); <code>maxConnectorLength</code> caps how far that may stretch before the label is omitted instead.</p><h2 id="collision-index" tabindex="-1">Collision index <a class="header-anchor" href="#collision-index" aria-label="Permalink to &quot;Collision index&quot;">​</a></h2><p>All occupied screen rectangles (atoms, bonds, placed labels) are kept in a uniform spatial hash with <code>spatialCellSize</code>-pixel cells, so each candidate test only inspects nearby occupants instead of the whole scene. This is what keeps quality placement near-linear in the number of labels.</p><h2 id="repair" tabindex="-1">Repair <a class="header-anchor" href="#repair" aria-label="Permalink to &quot;Repair&quot;">​</a></h2><p>Greedy placement can paint itself into a corner: an early label occupies the only good spot of a later one. <strong>Displacement repair</strong> fixes this by backtracking — when a label finds no valid candidate, the placer tries moving already-placed neighbours to their alternative candidates to free space, up to <code>repairDepth</code> levels deep and at most <code>repairSearchLimit</code> candidate evaluations. Setting either to 0 disables repair (<code>performance-omit</code> never repairs).</p><h2 id="quality-vs-performance-placement" tabindex="-1">Quality vs. performance placement <a class="header-anchor" href="#quality-vs-performance-placement" aria-label="Permalink to &quot;Quality vs. performance placement&quot;">​</a></h2><p>Two strategies share this machinery:</p><ul><li><strong>Quality placement</strong> (<code>quality-omit</code>) evaluates the full candidate set per label with repair. Best layouts, cost grows with density.</li><li><strong>Performance placement</strong> (<code>performance-omit</code>) walks atoms front-to-back and reuses <strong>no-space tiles</strong>: the screen is divided into <code>performanceNoSpaceCellSize</code>-pixel tiles, and when a front atom proves a tile has no room, deeper atoms in the same tile are omitted without their own search. Larger tiles reuse more of these negative results (faster) but may omit more deeper labels. Tiles are zoom-specific and rebuilt after camera changes. No repair is attempted.</li></ul><p>The default <strong><code>auto-omit</code></strong> picks between them per layout: quality placement for ordinary workloads, performance placement once more than <code>autoPerformanceLabelThreshold</code> requested labels are visible in the current projection (so zooming can switch the policy in either direction).</p><h2 id="maximum-coverage-and-callouts" tabindex="-1">Maximum coverage and callouts <a class="header-anchor" href="#maximum-coverage-and-callouts" aria-label="Permalink to &quot;Maximum coverage and callouts&quot;">​</a></h2><p><code>maximum-coverage</code> mode answers a different question: <em>label as many atoms as possible</em>, accepting longer connectors — the mode for annotated figures. It searches <code>maximumCoverageDistanceSteps</code> local radial distance bands around each atom (more bands give repair additional moderate placements at added worker cost), and when local space runs out it falls back to <strong>callouts</strong>: labels arranged in up to <code>calloutColumns</code> lanes just outside the projected structure (<code>calloutPlacement: &#39;structure&#39;</code>, clearance <code>calloutGap</code>) or along the viewport edges (<code>&#39;viewport&#39;</code>), spaced by <code>calloutColumnGap</code>/<code>calloutRowGap</code>, each connected to its atom by a leader line. Callout search is bounded by <code>calloutSearchLimit</code> candidates examined and <code>calloutChoiceLimit</code> retained per label.</p><h2 id="workers-throttling-and-indicators" tabindex="-1">Workers, throttling, and indicators <a class="header-anchor" href="#workers-throttling-and-indicators" aria-label="Permalink to &quot;Workers, throttling, and indicators&quot;">​</a></h2><p>The structure is always painted first; layout runs afterwards so it never delays the first frame. With <code>useWorker: true</code> (default), collision placement runs in a Web Worker and falls back to the main thread where workers are unsupported or fail.</p><p>During interaction, relayouts are throttled to <code>layoutThrottleMs</code>; layouts larger than <code>interactionLabelLimit</code> labels are deferred until the interaction ends, and <code>hideLabelsDuringDeferredLayout</code> clears the stale frame immediately so old labels never ghost over the new pose. If an asynchronous layout takes longer than <code>loadingIndicatorDelayMs</code>, an accessible status indicator appears (<code>showLoadingIndicator</code>).</p><h2 id="every-knob" tabindex="-1">Every knob <a class="header-anchor" href="#every-knob" aria-label="Permalink to &quot;Every knob&quot;">​</a></h2><p>The complete generated table of <code>atomLabels.*</code> options, with defaults, is in <a href="./../reference/atom-labels.html">Options Reference → Atom labels</a>.</p>',23)])])}const u=a(n,[["render",i]]);export{m as __pageData,u as default};
